{
  "name": "steez-browse",
  "version": "0.1.0",
  "description": "Personal browser binary — Browse superset with Playwright features and NetSuite automation",
  "license": "MIT",
  "type": "module",
  "bin": {
    "browse": "./dist/browse"
  },
  "scripts": {
    "build": "bun build --compile src/core/cli.ts --outfile dist/browse && bash scripts/build-node-server.sh && git rev-parse HEAD > dist/.version || true",
    "dev": "bun run src/core/cli.ts",
    "server": "bun run src/core/server.ts",
    "test": "bash scripts/test-files.sh src/core/test src/ns/test",
    "test:core": "bash scripts/test-files.sh src/core/test",
    "test:ns": "bash scripts/test-files.sh src/ns/test"
  },
  "dependencies": {
    "diff": "^7.0.0",
    "playwright": "^1.58.2"
  },
  "engines": {
    "bun": ">=1.0.0"
  }
}
