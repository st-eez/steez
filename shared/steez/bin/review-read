#!/usr/bin/env bash
# review-read — read review log and config for dashboard
# Usage: review-read
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
eval "$("$SCRIPT_DIR/slug" 2>/dev/null)"
STEEZ_HOME="${STEEZ_HOME:-$HOME/.steez}"
cat "$STEEZ_HOME/projects/$SLUG/$BRANCH-reviews.jsonl" 2>/dev/null || echo "NO_REVIEWS"
echo "---CONFIG---"
"$SCRIPT_DIR/config" get skip_eng_review 2>/dev/null || echo "false"
echo "---HEAD---"
git rev-parse --short HEAD 2>/dev/null || echo "unknown"
